# 3d-mahjong

## Development shortcuts

- `scripts/game_state.gd`: set `TIMER_DURATION_SECONDS` to change the gameplay timer duration in seconds. The 15 minute value is `900.0`.
- `scripts/game_state.gd`: set `USE_DEV_LEVELS` to switch between `data/levels_dev.json` and `data/levels.json`.
